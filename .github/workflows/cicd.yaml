name: CI/CD (ECR -> EC2)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

env:
  AWS_REGION: ap-south-2
  CONTAINER_NAME: incident-commander
  HOST_PORT: "8000"
  CONTAINER_PORT: "8000"

jobs:
  build_push_deploy:
    runs-on: ubuntu-latest
    env:
      ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
      PYDANTIC_AI_MODEL: ${{ secrets.PYDANTIC_AI_MODEL }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      EC2_HOST: ${{ secrets.EC2_HOST }}
      EC2_USER: ${{ secrets.EC2_USER }}
      EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
      EC2_ENV_FILE: ${{ secrets.EC2_ENV_FILE }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: python -m pip install --upgrade pip && pip install -r requirements.txt

      - name: Run tests
        run: python test_demo.py

      - name: Validate required secrets
        run: |
          missing=0
          for name in AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY ECR_REPOSITORY EC2_HOST EC2_USER EC2_SSH_KEY; do
            if [ -z "${!name:-}" ]; then
              echo "::error::Missing required GitHub Secret: $name"
              missing=1
            fi
          done
          if [ "$missing" -ne 0 ]; then
            exit 1
          fi
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify AWS identity
        run: aws sts get-caller-identity

      - name: Ensure ECR repository exists
        run: |
          aws ecr describe-repositories --repository-names "$ECR_REPOSITORY" >/dev/null 2>&1 || \
          aws ecr create-repository --repository-name "$ECR_REPOSITORY" >/dev/null

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set image URI
        run: |
          echo "IMAGE_URI=${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}" >> "$GITHUB_ENV"
          echo "IMAGE_URI_LATEST=${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:latest" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_URI }}
            ${{ env.IMAGE_URI_LATEST }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy to EC2 (SSH)
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE_URI: ${{ env.IMAGE_URI }}
          AWS_REGION: ${{ env.AWS_REGION }}
          CONTAINER_NAME: ${{ env.CONTAINER_NAME }}
          HOST_PORT: ${{ env.HOST_PORT }}
          CONTAINER_PORT: ${{ env.CONTAINER_PORT }}
          EC2_ENV_FILE: ${{ env.EC2_ENV_FILE }}
          PYDANTIC_AI_MODEL: ${{ env.PYDANTIC_AI_MODEL }}
          OPENAI_API_KEY: ${{ env.OPENAI_API_KEY }}
        with:
          host: ${{ env.EC2_HOST }}
          username: ${{ env.EC2_USER }}
          key: ${{ env.EC2_SSH_KEY }}
          port: 22
          envs: IMAGE_URI,AWS_REGION,CONTAINER_NAME,HOST_PORT,CONTAINER_PORT,EC2_ENV_FILE,PYDANTIC_AI_MODEL,OPENAI_API_KEY
          script: |
            set -eu
            echo "Deploying $IMAGE_URI"

            if ! command -v docker >/dev/null 2>&1; then
              echo "docker is not installed on the EC2 host" >&2
              exit 1
            fi

            if ! command -v aws >/dev/null 2>&1; then
              echo "aws cli is not installed on the EC2 host (required for ECR login)" >&2
              exit 1
            fi

            ECR_REGISTRY="$(echo "$IMAGE_URI" | cut -d/ -f1)"
            aws ecr get-login-password --region "$AWS_REGION" | docker login --username AWS --password-stdin "$ECR_REGISTRY"

            docker pull "$IMAGE_URI"

            if docker ps -a --format '{{.Names}}' | grep -qx "$CONTAINER_NAME"; then
              docker stop "$CONTAINER_NAME" || true
              docker rm "$CONTAINER_NAME" || true
            fi

            ENV_FILE_ARGS=""
            if [ -n "${EC2_ENV_FILE:-}" ]; then
              if [ ! -f "$EC2_ENV_FILE" ]; then
                echo "EC2_ENV_FILE does not exist: $EC2_ENV_FILE" >&2
                exit 1
              fi
              ENV_FILE_ARGS="--env-file $EC2_ENV_FILE"
            fi

            SECRET_ENV_ARGS=""
            if [ -n "${PYDANTIC_AI_MODEL:-}" ]; then
              SECRET_ENV_ARGS="$SECRET_ENV_ARGS -e PYDANTIC_AI_MODEL=$PYDANTIC_AI_MODEL"
            fi
            if [ -n "${OPENAI_API_KEY:-}" ]; then
              SECRET_ENV_ARGS="$SECRET_ENV_ARGS -e OPENAI_API_KEY=$OPENAI_API_KEY"
            fi

            # shellcheck disable=SC2086
            docker run -d --restart unless-stopped \
              --name "$CONTAINER_NAME" \
              -p "$HOST_PORT:$CONTAINER_PORT" \
              $ENV_FILE_ARGS \
              $SECRET_ENV_ARGS \
              "$IMAGE_URI"

            docker image prune -f >/dev/null 2>&1 || true
